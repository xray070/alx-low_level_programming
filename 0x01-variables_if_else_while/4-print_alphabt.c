#include <stdio.h>
/**
 * main - alphabet in lowercase except q and e
 * Return: 0
 */
int main(void)
{
	char = 'a';
	while (ch <= 'z')
		ch++;
	{
		if ((ch != 'a') && (ch != 'q'))
			putchar(ch);
	}
	putchar('\n')
		return (0);
}
