#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Generate k
 * Return: 0
 */
int main(void)
{
	int n;
	int k;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (n > 0)
		{
			printf("%is 
	return (0);
}
